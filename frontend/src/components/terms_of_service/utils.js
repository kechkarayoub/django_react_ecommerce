
export const render_term_service_custom_text = (text) => {
    return `<b>${text}</b>`
}

export const render_term_service_url = (url_link, url_label) => {
    return `<a href='${url_link}' target="_blank" style="color: #1fa1cf;">${url_label}</a>`
}
